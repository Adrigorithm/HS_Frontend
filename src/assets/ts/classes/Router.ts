class RouterImpl {
    readonly pages: Array<string> | string;

    constructor(pages: Array<string> | string){
        this.pages = pages;
    }

    navigate(idName: string): void {
        
    }
}